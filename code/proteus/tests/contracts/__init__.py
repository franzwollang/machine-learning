"""Typed I/O contracts matching the Proteus SI specification.

These dataclasses define the public shapes of Proteus objects so that
tests can be written before implementation modules exist.  When
implementation lands, these become the canonical public types.
"""
