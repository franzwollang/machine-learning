"""Slow-marked persistence scale-search pins (split from test_scale_search_persistence).

A6-T4 / #28 / #46: pinned experiments live here; ``test_core_smoke.py`` is the
unmarked default smoke slice (A6-T5).
"""
