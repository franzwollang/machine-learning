# Ensemble rho-SFCs

Implementation home for the Ensemble rho-SFCs work.

This folder is intentionally separate from `code/proteus/` so each paper/project can evolve with its own package layout and tests.
