{
  "updated_at": "2026-07-24T06:40Z",
  "turn": 6,
  "focus": { "milestone": "M2", "issue": 28, "goal": "retire the load-band heuristic; land a principled, dominant tau* selector" },
  "recent": [
    "ff-merged chain head 7b9a into branch 6084 (M0 done, M1 in prog, M2 persistence). Env: pip install numpy scipy pytest scikit-learn; PYTHONPATH=src python3 -m pytest -m 'not real_data and not slow'. Baseline 119 passed/31 xfailed.",
    "Instrumented traces: compensated node-count N*tau^{d/2} is confounded by N_max saturation (hierarchical caps at 128 everywhere, swiss caps mid-grid) -> NOT a universal selector.",
    "variance-load=cap crossing (Lbar=1, log-interp of coarsest stabilized bracket) is clean & DOMINATES band on tau* (circle 8.0->1.13, swiss 1.88->0.73, var_density/nested ~1.9 vs 8.0). Added load_crossing+combined selectors behind flag; 5 tests assert dominance; SI+issues+S14.3 done; 124 passed. Commits pushed."
  ],
  "discoveries": [
    "Primary tau* signal = Lbar(tau)=E[sigma^2/tau] crossing the cap (=1, derived not tuned); documented SI S2.5.1. Compensated node-count/support-trace is a budget-confounded diagnostic only (recorded in #28).",
    "combined selector (SI S2.6.2): persistence scale if a multi-cluster feature persists (splittable region), else load-crossing operating scale, else legacy fallback -- unifies tau* selection with recursion timing.",
    "run_scale_search off-grid tau_star is safe: recursion.py only reads tau_star + scaffold_at_star, which is re-converged at tau_star. peak_index set to nearest grid idx."
  ],
  "open_threads": [
    "#28 default flip: migrate test_recursion/test_circle/test_swiss_roll/test_hierarchy configs to selector='combined' one at a time (confirm tau*/leaf stability), THEN flip ScaleSearchConfig.selector default load_band->combined and delete _select_characteristic_scale/_legacy_slope_selector/_detect_peak (band_lo).",
    "M1 #27 Part B still pending: wire persistence as the split accept-gate in recursion/clustering (accept split only if persists>=P_persist), confirm circle=1/swiss<=3/hierarchy 6-leaf, THEN delete clustering.py stand-in heuristics.",
    "chain PRs off main all unmerged; branch 6084 now carries the full chain + this M2 primary-selector work."
  ],
  "next_step": "Migrate one scenario/recursion config to selector='combined', verify green, then proceed toward the default flip + band_lo deletion (#28 items 1-2).",
  "blockers": []
}
