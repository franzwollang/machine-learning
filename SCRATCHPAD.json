{
  "updated_at": "2026-07-24T13:20Z",
  "turn": 18,
  "focus": { "milestone": "M2", "issue": null, "goal": "#31 RESOLVED: per-frame tau in hierarchy-recovery harness + tighten gates vs canonical tau*I (S2.5.4)" },
  "recent": [
    "CHAIN: git fetch first; furthest branch = 0276 (PR#17, tip 044a4f8, turn17 M3-closed). ff-merged into cursor/next-task-progression-f06e (was clean off main). diff vs head empty.",
    "#31 RESOLVED: assert_recursion_matches_gt_hierarchy_unimodal_levels now uses per-matched-node tau_star (resolve up parent chain; memoized _analytic_for per scale) instead of single global root tau. Hungarian matching kept at root-tau analytic (GT means are convolution-invariant).",
    "Gates tightened vs canonical tau*I (S2.5.4, #32 done): mean_chi2_alpha 0.02->0.05, cov_fro_atol 0.55->0.45, cov_fro_rtol 0.28->0.12. Measured root margins: mean 2.6x, cov 3.7x. Docstrings drop 'provisional §32' -> S2.5.4 canonical.",
    "Added unit test test_unimodal_harness_uses_per_frame_tau (test_recursion.py): synthetic 3-level GT + tree; raises at fine tau_leaf=0.01 (Hotelling~56), passes at coarse tau_leaf=1.0 (~1.1) -> locks per-frame tau. Suite 159p/1skip/31xf. GPT (gpt-5.4-high) cross-family audit LGTM."
  ],
  "discoveries": [
    "Hierarchical-Gaussian recursion tree is FLAT (root->6 fine leaves at level 1); GT has 3 levels. So recursion leaves ~ GT FINE (L2), NOT GT coarse (L1). Level-indexed harness only aligns at root -> tests evaluate levels={0} only. Deeper end-to-end eval not exercisable on these fixtures (per-frame path validated by the unit test).",
    "Fine-leaf single-component moment gates FAIL badly (Hotelling 30-229, cov rel 2-5x): BMU-partition boundary leakage, not a bug. So #31 was NOT about adding a fine-leaf gate (that'd need loosening); it's per-frame tau + tightening the aggregate root gate."
  ],
  "open_threads": [
    "M1 #27 Part B blocked SOLELY on M4 S3.4 DM evidence gate (cold-start recheck REFUTED; do not re-attempt).",
    "M2 #28 remaining: refine persistence tau* off coarse-END; delete legacy load_band + dormant _legacy_slope_selector/_detect_peak once load_crossover dominates all scenarios.",
    "M5 S8.4/S4.2 must invoke refresh_intrinsic_dim explicitly on scale-consistent sample (SI S1.4.1, #40)."
  ],
  "next_step": "Next turn: fetch --all + chain onto furthest branch first. Candidates: M4 step1 flag-complex (S4.1-4.2, whole part; BIGGEST critical-path lever, unblocks M1 Part B via S3.4) OR M2 #28 cleanup (delete legacy load-band once dominance shown; persistence coarse-end refinement).",
  "blockers": ["#27 Part B blocked on M4 S3.4 DM evidence gate"]
}
